# v4.1.0 — session closure

**Date:** 2026-07-30 · **Branch:** `gauntlet/v4.0.0` → PR [#4](https://github.com/preql-data/claude-workflow-plugin/pull/4) · **Tag:** `v4.1.0`
**Status:** shipped, tagged, pushed. **Merge deliberately left to the operator.**

This document is the session record: what shipped, what was cut and why, what is still open, and what is not true that a reader might assume. It is written to be checkable — every number here came from a command run in this session, and where a number has changed since it was recorded elsewhere, both values are given.

---

## 1. One decision needed from the operator

**The `v4.1.0` tag points at `57fb888`, whose tree fails `installer-v3-upgrade` section 8e.** The fix landed afterwards in `b04870b`.

Precisely: **there is no CI run for `57fb888` itself.** CI fires on `pull_request` and binds to the pushed tip; `252f720` was committed 42 seconds later and **both were pushed together**, so the red run is [`30553650983`](https://github.com/preql-data/claude-workflow-plugin/actions/runs/30553650983), on `252f720`. That commit differs from the tag only in `.beads/issues.jsonl`, and `installer-v3-upgrade.sh` is the same blob (`2bde945`) in both, so the tagged tree fails identically — but a reader checking "CI on the tag" finds nothing.

8e asserted behaviour this release deliberately replaced (see §6). The fix is test-only; `install.sh` was never wrong and was never touched.

| Option | Consequence |
|---|---|
| **Move the tag to the current head** | Points at a green build. Requires a force-push of the tag **and a manifest regeneration** — see the prerequisite below. Nothing has consumed it; it is hours old on an unmerged branch. |
| **Leave the tag at `57fb888`** | Honest history, and the frozen manifest is *correct* for that tree. But `v4.1.0` permanently names a commit whose tests fail on checkout. |
| **Cut `v4.1.1`** | Cleanest semantically; the delta is a test-file correction plus the manifest, which is thin but not empty. |

> **PREREQUISITE, and it is not optional.** `manifests/v4.1.0.sha256` was frozen at `57fb888`. `b04870b` appended a lesson, so `LESSONS.md` — an `operator`-class row in that table — no longer matches its recorded hash (`296ba96f` in the table vs `275a8441` on disk, row 130). **If the tag moves without regenerating the manifest, the release ships a table that misclassifies its own `LESSONS.md`**, landing on `preserve-custom` per `workflow-manifest.sh:490-503` and writing a spurious `LESSONS.md.new` on every later upgrade — for a file the operator never touched. That is the same defect class `016` just fixed, reintroduced by the commit that fixed it. Tracked as `ce5`.

**Recommendation: move the tag, and regenerate the manifest in the same operation.** A release tag naming a failing tree is the worse artifact. Not done unilaterally because moving a pushed tag is outward-facing and the manifest regeneration is a reviewable change.

---

## 2. What shipped

**32 commits** between `v4.0.0` and the release commit. Every phase carries a change-set-bound approval reviewed by `qa` against a `devops` implementer.

### The unplanned P0 — installed targets did not orchestrate

Not in the original directive; it took priority over everything else. Projects that curl-installed v4 **ran no workflow at all** and had **both MCP servers dead**, while the same v4 worked inside the plugin's own repo.

The directive expected one packaging failure. **Evidence separated it into two independent bugs**, and eliminated agent registration as a cause entirely.

| Task | Commit | What it fixed |
|---|---|---|
| C0a `0fc` | `ff38ee8` | `workflow-doctor.sh` — 11 named checks, sandboxed, exact MCP tool counts (21/7). Replaced a post-install verification that was literally two `test` calls. |
| C0b `z9m` | `591dbb8` | **Symptom 2**: under `curl \| bash` the `--depth 1` clone never *had* `node_modules` to copy, and `node` was not a checked prerequisite. Deps now install in the target. |
| C0c `20e` | `33ab228` | **Symptom 1**: SessionStart exited with a bare `{"error":…}` and no envelope, so Claude ran with the plugin installed and its rules absent. Now degrades loudly. |

**The proof is the instrument turning on itself:** `make install-test` went from **9 passed / 2 failed → 11 / 0**. The check built in C0a to detect the bug now certifies its absence.

**Six defects were found *inside* the P0 fixes**, each caught by the check the previous one taught us to run:

1. A falsified non-mutation claim in the doctor (round-1 block).
2. `npm ci` itself deletes `node_modules` before installing — so the data-loss fix reopened the same harm on the default path, for **100% of re-installs** rather than ~0%.
3. A *preserved* failure ended in an unqualified green `Installation complete.` with both servers advertised, walking around the exit-3 contract written to prevent exactly that.
4. **The husk**: a failed `npm ci` leaves 94 empty directories, so `[ -d node_modules ]` answers true for a tree that cannot boot. Introduced *while fixing* #3, caught by the negative control. Both arms exit 3, so no exit code discriminates — that defect family lives entirely in the readout.
5. A third context-losing path nobody had enumerated: `set -e` plus a missing `awk` kills SessionStart at rc=127 with zero stdout.
6. A README offering `grep -n 'npm ci' install.sh` as proof, where that literal occurs zero times.

### The planned phases

| Phase | Epic | Commit | Outcome |
|---|---|---|---|
| U0 installer upgrade path | `0jk` | (8 sub-tasks, pre-session) | **Shipped** in full |
| U1 follow-up burn-down | `waz` | `gz3`, `gl6`, `bjx` | **Shipped** |
| U2 sweeper + denylist | `0yg` | `f73c46c`, `b10c1f3` | **Shipped**, both children |
| U3 context ledger | `gio` | `e85e5dd` | **Reduced** — see §3 |
| U4 superpowers skills | `q37` | `05b13ba` | **Reduced** — see §3 |
| U5 staging e2e QA | `f2t` | — | **Cancelled** — see §3 |
| U6 depth-3 migration | `7be` | — | **Closed, need-triggered** — see §3 |
| C5 release | `uvk` | `57fb888` | **Shipped** |
| C7 CI fix | `016` | `b04870b` | **Shipped** |

---

## 3. What was cut, and why

**U5 — staging e2e QA: CANCELLED.** Four reasons, ranked: most external dependencies of any phase; largest security surface, since injected auth means credentials in a harness the plugin drives unattended; the only phase requiring live paid runs, so its evidence would have been the weakest in a release whose standing rule is *no adjective without an artifact*; and the provider abstraction is speculative until a **second** project validates the shape — building from one project's needs would encode that project's assumptions as the interface.

**Not deferred on a timer. There is no date at which this becomes correct.** It becomes correct when a second project needs staging QA, which is also the evidence that would tell us what to build.

**U3 — reduced to a contract field.** The motivating problem (a 14-PR speculative-fix chain) is already solved by evidence-before-fix, which is shipped and working. Building a ledger to detect a class of failure another mechanism already prevents is surface without a defect to justify it. The harness design is preserved in full on `gio.1`, **deferred behind an evidence bar**: name three closed tasks whose `context_coverage` omitted the same class of source. Evidence first, machinery second.

**U4 — reduced to one vendored reference doc** plus a practice harvest, rather than five registered skills.

**U6 — CLOSED need-triggered; the 2026-08-23 date is RETIRED.** Depth-1 is not a limitation awaiting removal — **it is an enforcement mechanism.** Because subagents cannot spawn subagents, an implementer *structurally cannot procure its own review*; grader and reviewer spawns must originate at the root, which is precisely the separation the gate's independence predicate guarantees. Raising the pin would mean writing a parent-role guard to restore in software what the platform gives for free, to enable nesting no workflow here needs. Standing task `1bn` states the trigger as a **condition, not a date** — the platform default has moved three times in this project's lifetime, so a calendar check would have fired against whichever value happened to be current and told us nothing.

---

## 4. Numbers, and where they disagree

| Tier | At release (`57fb888`) | Now (`3a86e4f`) |
|---|---|---|
| L1 | 32 specs / 32 passed | **32 / 32** |
| L2 | 41 specs / 1,983 assertions | **41 / 41 specs, 2,002 assertions** |
| e2e unit | 444 passed / 5 skipped | 444 / 5 |
| lint | clean | clean |

**CI on `3a86e4f`: `OVERALL: success`** — L1, L2, L3-vitest and manifest validation all pass; L3-live is `skipped` (manual, paid). The one red job is `shellcheck (advisory)`, which is `continue-on-error` and has been permanently red since before this release: it runs a stricter invocation than `make lint` and flags info-level findings on deliberate idioms (`SC2317` on trap handlers, `SC2015` on `A && B || C`). Tracked as `hbr` — **a check that can never go green is indistinguishable from a newly-broken one**, which is the same family as §7.

**Where the assertion count moved, and why — stated carefully, because the convenient explanation is wrong.** The release notes record 1,983 assertions; it is now 2,002. **The entire +19 is `installer-v3-upgrade.sh` going 235 → 254** as the C7 fix strengthened section 8e. It is deterministic and reproducible, not flake. Attributing it to the flaking spec would make a solid number look unstable.

**Separately**, `code-graph-mcp.sh` failed in two runs and passed in two others — **it flakes in both directions**, contradicting its own original filing. Tracked as `dxz` (P2), with a narrowed mechanism: `code_search` demonstrably builds a working index, yet `code_index_health` finds no database at `indexPath()`. Not caused by anything in this release; it reproduces at byte-pristine HEAD. **Because of it, a re-measurement may not land on 2,002** — treat that as a figure with a tracker attached, not a constant.

**Other measured facts:** 16 audit rows (`UW1`–`UW16`), 3 of them `PROVEN-WITH-CAVEAT`. `manifests/v4.1.0.sha256` at 132 rows; regenerate-and-`cmp` is byte-identical **at `57fb888`** and under all three locales tested — but **differs at HEAD** on row 130 (`LESSONS.md`), which is the §1 prerequisite. **71 lessons** in the ledger, **25 added this session** (45 → 70, plus the 71st in `b04870b`), **48 since the `v4.0.0` tag**.

> **Two corrections to artifacts produced earlier in this session.** The Slack draft says "18 lessons added" — wrong; it is 25 this session and 48 since v4.0.0. And an earlier draft of this document said 70 / 47, measured two commits before it shipped. **The lesson generalises: a document carrying its own verify commands must be measured at the commit that ships it.**

---

## 5. Caveats — what is NOT true

These are stated because a reader could reasonably assume otherwise.

**`install.ps1` has never been executed by anyone, anywhere.** No PowerShell host on the development machine, and `gh run list --workflow windows-install.yml` returns **zero runs**. Every v4.1 mechanism in it — the `npm ci` block, the three-arm headline, the husk predicate, `Copy-ShippedTree` — is verified only by text parity against the bash side. Treat Windows as untested. Tracked as `6vc`.

**`context_coverage` adds zero mechanical runtime enforcement.** The parity test guards *documents*; nothing rejects a payload at runtime, and e2e Invariant 3 remains `skipped` on its documented trace gap. Shipped as `PROVEN-WITH-CAVEAT` with the caveat inline.

**The brainstorming ceremony skip is prompt-level only.** F1 is a Stop-time *change-set* classifier and structurally cannot gate whether a document was read before decomposition.

**The QA gate is a strong guardrail, not a sandbox.** It is hooks and records; a determined actor can route around it.

**A frozen audit row was false when written.** Row `A10` claimed all specialist prompts carried the six-field schema (read 2026-06-13) — `devops.md` had no contract section *at all* until C2. Left byte-untouched and superseded by `UW14`, with an addendum sentence distinguishing *"frozen because dated"* from *"frozen despite being wrong"*, so a dated row is not read as verified merely for carrying a date.

---

## 6. The CI break, and the process gap that caused it

`manifests/v4.1.0.sha256` was written by shell redirection, so `post-edit.sh` never saw it and **it never entered a change-set hash** — a new file reached a release commit without any reviewer's change set naming it. C5's reviewer then scoped its re-run by grepping for readers of the two *edited* files.

**Both decisions were locally reasonable. Together they let a regression reach a tag.**

There is a sharper edge than "nobody grepped the new file": `install.sh:1317` reads it by **constructed** name (`manifests/v$V3_DETECTED_VERSION.sha256`), so a literal grep for `v4.1.0.sha256` would have found nothing anyway. **The check that would have caught this is "who globs or constructs paths under `manifests/`", not "who names this file."**

The fix was also larger than the four red assertions, for a reason worth recording: because both table sources now describe the same release, the subject/control pair **stopped discriminating on outcome** — it differed only in which table got *named*. Correcting the assertions alone would have left the section weaker than before *while showing green*. Proven by mutation: stripping the manifest-preference makes the naming assertions fail **while the count assertions stay green**.

---

## 7. The release's meta-finding

The plugin gates itself, so its own signals are load-bearing. This release found **six ways they claimed more confidence than their evidence supported**:

| ID | Finding |
|---|---|
| `qzv` | F1 stamps `qa-approved` on a task whose work does not exist, closes it, and clears `current-task`. **Fired on the release task itself**, 22 s after the implementer spawned, bound to a *previous* task's change set. |
| `94d` | The change-set tracker under-covers, so an approval can bind a smaller set than the diff. |
| `1nz` | Live-repo assertions fail under any concurrent writer — **five instances**, the last being the Stop hook racing the gate's own state machine. |
| `2ty` | The J21 counter charges a thorough review against the defect budget, and auto-defers. |
| `hbr` | An advisory CI job is permanently red — indistinguishable from newly-broken. |
| `A10` | A frozen audit row was false when written, not stale. |

Each reads as a nit alone. Together they are one finding, now recorded in `LESSONS.md`:

> **A gate that reports confidently on unverified evidence is worse than no gate, because it is trusted.**

**The containment is the good news, and it is real.** When `qzv` fired on the release task, v4's change-set binding meant the Stop hook would emit `LABEL_WITHOUT_RECORD` and re-block rather than release on the stale label — and `plugin.json` is reviewable source under F1's own classifier, so it could not re-fire. **v3 would have released on this; v4 did not.** The damage was to the record, not the release.

It was caught only because `bd_update_task` echoed back `status=closed, labels=[qa-approved]` when the implementer had passed neither. **Specialists re-reading task state at completion would have caught all four instances** — cheap, and worth adopting.

---

## 8. Open follow-ups

None blocks the release. Filed with evidence, reproduction steps, and candidate fixes.

**P1 — gate integrity**
- `qzv` — F1 binds its verdict to the change set but writes its label to the task. Four recorded consequences.
- `94d` — subagent edits and shell-written files never reach `changed-files.txt`.

**P2**
- `1nz` — live-repo assertions vs concurrent agents. Five instances; only the double-snapshot control survives a gate that mutates state while the Stop hook fires each turn.
- `dxz` — `code-graph-mcp` flakes in both directions; `indexPath` disagreement is the lead.
- `8zi` — `approve` does not clear `qa-blocked`, so every block→fix→approve cycle ends contradictory.
- `e2j` — a ban list cannot detect deletion; the vendored file can be gutted to a stub with the spec still green.
- `igp` — server READMEs offer a grep recipe that returns only comments.
- `qql` — a test leg derives its expected *name* from source state while hardcoding its *outcomes*, so it protects half of what its comment claims.
- `ce5` — the frozen manifest is stale at HEAD; §1's prerequisite.

**P3 — hygiene and coverage**
`mn7`, `vtq`, `6vc`, `nnr`, `go7`, `kya`, `aqp`, `e29`, `bi1`, `eo8`, `nsd`, `buc`, `szn`, `l2g`, `fl5`, `2ty`, `hbr`

**P4 / deferred**
- `1bn` — raise the spawn-depth pin **when** a workflow needs it.
- `gio.1` — the U3 harness, behind a three-instance evidence bar.

**Pre-existing, untouched: 23 tasks sit in `in_progress`**, all from v3-era work — `llh.23`, `y4a.13`, `y4a.14`, and `0wk.1` plus `0wk.9`–`0wk.29` *excluding `0wk.24` (closed) and `0wk.27` (open, not `in_progress`)*. Several date to 2026-05-09. Not this release's scope, but the count is large enough that `bd list --status in_progress` is no longer a useful signal, which is itself worth a triage pass.

---

## 9. Deliverables outside the repo

- **PR #4** — title and body written (19,868 chars), including the phase-outcome table and the **19-row practice-harvest table**. That table exists *only* there. `l2g` tracks landing it somewhere tracked; **its destination is undecided** — `.claude/vendor/superpowers/MANIFEST.md` is the natural home, since it already carries the ten per-modification rows, but `l2g` currently names none, and there is no root `MANIFEST.md`. The **"48 rows" figure appears in several tracked places** (`docs/RELEASE_AUDIT.md:679`, `docs/plans/v4.1-upgrade-wave.md:18`, `.beads/issues.jsonl`, and `05b13ba`'s body) — what does not exist is a 48-row *table*. Two agents independently failed to count it against any artifact, so a 19-row reconstruction from tracked evidence shipped instead of matching an unverifiable number. `RELEASE_AUDIT.md:688` words this correctly: *"the figure '48' is unverified — it could not be counted against any artifact."*
- **Slack update** — drafted in-session, house style, **not posted**. Leads with the action teammates need: anyone who ran the v4.0.0 curl install must re-run it. Fix the lessons count before posting (§4).

---

## 10. Verify this document

```bash
git log --oneline v4.0.0..v4.1.0 | wc -l         # 32 (the release); v4.0.0..HEAD is larger — post-tag fixes
jq -r '.version' .claude-plugin/plugin.json      # 4.1.0
bash install.sh --help | head -1                 # Claude Workflow Plugin v4.1.0 installer
bash .claude/scripts/tests/run-tests.sh          # Total: 32  Passed: 32
bash .claude/tests/component/run.sh              # Specs Total: 41 — assertions vary, see §4 (dxz)
grep -c '^- ' LESSONS.md                         # 71
awk '/^## v4\.1\.0 claims ledger/,0' docs/RELEASE_AUDIT.md | grep -cE '^\| UW[0-9]+ \|'   # 16
git rev-list -n1 v4.1.0 | cut -c1-7              # 57fb888 — see §1
gh run list --workflow windows-install.yml       # zero runs — see §5
```

**One command deliberately NOT listed as passing**: `workflow-manifest.sh generate . | cmp - manifests/v4.1.0.sha256` is clean at `57fb888` and **differs at HEAD** on row 130. That is §1's prerequisite, not an error in the table.

`HANDOFF.md`'s v4.1.0 section carries the full `assert:` list, including the one that was load-bearing and previously unstated anywhere: **`plugin.json` must keep 2-space top-level indentation**, because `plugin_json_version()` (`install.sh:118-122`, the `sed` at `:120`) is a jq-free reader anchored on exactly two spaces. *`HANDOFF.md:294` cites `:96-101` for the same function; that reference is stale and should be corrected in both places or neither.*

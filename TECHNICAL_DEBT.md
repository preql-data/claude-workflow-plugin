# Technical debt

Deferred findings from QA gate runs. Each row was logged via
`.claude/scripts/tech-debt.sh add` (see plugin docs for J22 / Phase 4).

| severity | file:line | effort | description | added | resolved |
| -------- | --------- | ------ | ----------- | ----- | -------- |
| medium | .claude/scripts/verify-before-stop.sh:multiple | 2d | Mutation survivors (themes A-E, ids 1,2,3,6,8,9,11,13-24): block-reason wording, escalation boundaries, F1 doc-only fast path, git-status fallback, cross-repo detection. Backlog task claude-workflow-plugin-6ix. Verdict .claude/.mutation-runs/20260612T063107Z/verdict.json. Discovered-from claude-workflow-plugin-n45.3. | 2026-06-12 | 2026-06-13 (G2.6ix / claude-workflow-plugin-llh.5): re-swept against current code (.claude/.mutation-runs/20260613T102846Z + 112947Z + 120529Z). Behavioral survivors killed by L2 specs — ids 1,2,3,6,8,9,11,13,14,15,20,21 already killed by the llh.3/llh.2 rewrites' specs; ids 16-19 (lint/type wording) + 22,23,24 (escalated/SUITE_REUSED/J21 wording) + the NEW survivors (line 677 fast-path; 921,974,1016,1048,1114 escalation/QA-req wording; 976 J21 placeholder) killed by new assertions in specs/verify-before-stop.sh + specs/escalation-binding.sh. See the accepted-equivalents row below for the residue. |
| medium | .claude/scripts/post-edit.sh:multiple | 1d | Mutation survivors (theme F, ids 25,26,28,29,30,31,32): tracking-file trim threshold, every-10th edit cadence, CLAUDE_PROJECT_DIR default, sort/wc pipeline, EDIT_COUNT increment. Backlog task claude-workflow-plugin-6ix. Verdict .claude/.mutation-runs/20260612T063107Z/verdict.json. Discovered-from claude-workflow-plugin-n45.3. | 2026-06-12 | 2026-06-13 (G2.6ix / claude-workflow-plugin-llh.5): all 7 behavioral survivors (25,26,28,29,30,31,32) killed by new assertions in specs/post-edit.sh (trim threshold + boundary, every-10th cadence, CLAUDE_PROJECT_DIR + LINE_COUNT defaults, sort/wc pipeline, EDIT_COUNT increment) + a META-TEST. id27 accepted as equivalent (see below). |
| low | .claude/scripts/post-edit.sh:45, .claude/scripts/verify-before-stop.sh:914,1000,1011,1015,1159 | n/a (accepted) | ACCEPTED mutation survivors (G2.6ix / claude-workflow-plugin-llh.5) — not killed by design, with rationale: **EQUIVALENT** (no test can ever distinguish): post-edit.sh:45 `printf '%s' "$tid" \|\| true` (F3) — printf feeds a command substitution; its only failure mode is a pipe-write error unconstructible through the hook interface (rc 0 in every reachable state; matches the C.2 verdict's id27 classification). verify-before-stop.sh:914 & :1000 `mark_escalation_if_capped "${CURRENT_TASK:-}"` -> `${CURRENT_TASK}` (F4) — CURRENT_TASK is unconditionally assigned at L563, so the `:-` default is dead code. verify-before-stop.sh:1011 `printf '%s\n' "$f" \|\| true` (F3) — same unconstructible-pipe-write shape inside the FILTERED loop. verify-before-stop.sh:1015 `${CHANGE_COUNT:-0}` -> `${CHANGE_COUNT}` (F4) — CHANGE_COUNT is set by `grep -c .` on the prior line, which always emits a number. **BEHAVIORAL-BUT-LOW-VALUE**: verify-before-stop.sh:1159 epic shared-files "Shared-files notice" guard (`SHARED_COUNT -gt 0`, F1; + its `${SHARED_COUNT:-0}` F4 which is also dead since SHARED_COUNT is always set) — driving it requires a full multi-task-epic fixture (epic + in-progress sibling + overlapping changed-files + qa-approved completion) to assert an ADVISORY notice that does not change the gate decision; the cost is disproportionate to the value. NOTE: verify-before-stop.sh:976 (the sibling F4 `${CURRENT_TASK:-<TASK_ID_NEEDED>}` J21 placeholder) was NOT accepted — it IS killed (no-task cap-hit test in specs/escalation-binding.sh asserts the `<TASK_ID_NEEDED>` placeholder). Re-sweep run: .claude/.mutation-runs/20260613T130227Z. | 2026-06-13 | accepted (rationale recorded; not scheduled for a test) |

{
  "name": "@fixture/ui",
  "private": true,
  "version": "0.0.0",
  "description": "UI component package — the prompt asks Claude to add a RetryButton here.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}

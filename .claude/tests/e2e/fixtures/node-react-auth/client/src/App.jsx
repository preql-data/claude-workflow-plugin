// React stub. The fixture prompt will ask Claude to add a LoginForm
// component; today this is just an empty shell that mounts.

import React from "react";

export default function App() {
    return (
        <div className="app">
            <h1>node-react-auth fixture</h1>
            <p>Replace this placeholder with the LoginForm Claude is asked to build.</p>
        </div>
    );
}

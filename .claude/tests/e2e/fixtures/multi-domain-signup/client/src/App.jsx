// React shell for the multi-domain-signup fixture. The prompt asks
// Claude to add a <SignupForm> component as part of the end-to-end
// signup flow; that component will live alongside this stub and likely
// be referenced from here.
import React from "react";

export default function App() {
    return (
        <div className="app">
            <h1>multi-domain-signup fixture</h1>
            <p>Signup UI lands here once the @frontend specialist runs.</p>
        </div>
    );
}
